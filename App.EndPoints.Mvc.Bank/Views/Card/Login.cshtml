
<div class="page">

    <form class="login-form" method="post" asp-action="Login">

        <img src="~/pic/logo.png" alt="logo">
        <h5>Eghtesad Novin Bank</h5><br>


        <input placeholder="Enter NumberCard" type="text" name="cardNumber" value="" required><br>

        <input placeholder="Enter Password" type="password" name="password" value="" required><br><br>
        <button type="send" class="login-btn">Send</button>
       
        @if (ViewBag.SuccessMessage != null)
        {
            <div class="alert alert-success">@ViewBag.SuccessMessage</div>
        }
        @if (ViewBag.ErrorMessage != null)
        {
            <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
        }


    </form>
</div>
